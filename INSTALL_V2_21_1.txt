V2.21.1 corrected controlled full-frame capture delta
=====================================================

IMPORTANT CLEANUP FROM THE FIRST/BAD V2.21.1 ZIP
-------------------------------------------------
The first package accidentally placed App as a NEW ROOT FILE:

    ./app.py

That file is wrong and should be deleted:

    rm -f app.py

Do NOT delete src/engine/app.py.

Why:
    main.py imports:
        from src.engine.app import App

Therefore the actual application implementation belongs in:
    src/engine/app.py

This corrected ZIP patches that file and does NOT modify main.py.

INSTALL
-------
From the repository root, first remove the accidental root file:

    rm -f app.py

Then unpack this ZIP over the repository root, preserving directories.
It overwrites/adds:

    src/engine/app.py
    src/engine/scenes/automation_ai_training.py
    automation/autostart_ai_training.py
    INSTALL_V2_21_1.txt
    DETECTOR_V2_21_1.md
    PACKAGE_V2_21_1_MANIFEST.txt

Restart Skjutbana after installation because src/engine/app.py changes.

SMOKE TEST
----------
Start Skjutbana normally via main.py, then in another terminal run:

    python3 -m automation.autostart_ai_training white --iterations 10

Then audit:

    python3 -m automation.physical_pack_v221_inspect \
      --root content/ai/candidate_shadow_v216

If direct-ready full-frame shots are present, run:

    python3 -m automation.direct_proposal_v221_benchmark \
      --root content/ai/candidate_shadow_v216
